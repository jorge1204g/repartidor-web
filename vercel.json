{
  "buildCommand": "cd restaurante-web && npm ci && npm run build",
  "outputDirectory": "restaurante-web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
